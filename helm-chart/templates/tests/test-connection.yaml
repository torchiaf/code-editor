apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-vscode.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "open-vscode.name" . }}
    helm.sh/chart: {{ include "open-vscode.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "open-vscode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
