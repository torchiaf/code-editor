apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: server
spec:
  type: {{ .Values.server.type }}
  selector:
    app: server
  ports:
	{{- .Values.server.ports | toYaml | nindent 2 }}
